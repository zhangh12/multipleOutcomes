% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netbenefit_.R
\name{netbenefit_}
\alias{netbenefit_}
\title{Creating Objects of Hierarchical Net-Benefit (Win-Difference) Statistic}
\usage{
netbenefit_(formula, endpoints, data_index = 1)
}
\arguments{
\item{formula}{a two-sided R formula \verb{<label> ~ arm}. The LHS is used
purely as a row label in \code{pated()} output; the RHS must contain exactly
one variable, the column in \code{data} holding treatment-arm assignment.}

\item{endpoints}{a non-empty list of endpoint specs built by \code{nb_tte()},
\code{nb_continuous()}, or \code{nb_binary()}. The order of the list encodes the
hierarchical priority: endpoint 1 is checked first; if it ties, the
pair falls through to endpoint 2; and so on.}

\item{data_index}{integer. Index of the data frame in the \code{data}
argument of \code{jointCovariance} to be used.}
}
\value{
An object of class \code{c("jc_spec_netbenefit", "jc_spec")}.
}
\description{
\code{netbenefit_} creates an object to be passed into \code{jointCovariance} or
\code{pated} through its \code{...} argument. The object defines a hierarchical
net-benefit (a.k.a. win-difference, proportion-in-favor) statistic
across a list of endpoints, each declared by \code{nb_tte()},
\code{nb_continuous()}, or \code{nb_binary()}.
}
\details{
The estimator is
\deqn{\widehat\Delta = \frac{N_W - N_L}{N_W + N_L + N_T}}
where \eqn{N_W}, \eqn{N_L}, and \eqn{N_T} are the numbers of treatment
wins, losses, and overall ties across all \eqn{n_C \times n_T} pairs
(control vs. treatment subject). The per-subject influence function is
available in closed form, so both the asymptotic and the bootstrap paths
of \code{jointCovariance} are supported.

The arm reference level is inferred from the arm column the same way
\code{model.matrix(~ arm)} would: \code{levels(arm)[1]} for factor, the smaller
value for numeric or logical, and the alphabetically first value for
character. To override, convert the column to a factor with the desired
level order before calling \code{netbenefit_()}.
}
\seealso{
\code{\link[=nb_tte]{nb_tte()}}, \code{\link[=nb_continuous]{nb_continuous()}}, \code{\link[=nb_binary]{nb_binary()}}, \code{\link[=jointCovariance]{jointCovariance()}},
\code{\link[=pated]{pated()}}.
}
